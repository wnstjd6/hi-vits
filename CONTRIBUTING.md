# Contributing

빠른 가이드:

- 브랜치 네이밍: `feat/#<issue-number>-short-desc` (예: `feat/#12-create-post`)
- 커밋 메시지: gitmoji 사용 + 이슈 태그 포함 (예: `✨ feat: 게시글 생성 API 구현 (#12)`)
- PR: 이슈와 연결(Closes #12), 라벨 지정, 리뷰어 지정
- `main` 브랜치에 직접 push 금지 — PR로 병합

커밋 예시:
```
✨ feat: 게시글 생성 API 구현 (#12)
🐛 fix: 존재하지 않는 ID 조회 시 500 반환되는 문제 수정 (#15)
♻️ refactor: 서비스 계층으로 비즈니스 로직 분리 (#18)
```

PR 생성 시 체크리스트:
- 기능별 이슈에 연결되어 있나요?
- 입력 검증과 예외 처리 추가되었나요?
- README 또는 API 문서가 업데이트 되었나요?

검토 가이드(리뷰어용):
- 변경이 요구사항과 일치하는지 확인
- 경계 조건(입력 검증, 에러 상태) 검증
- DB 변경이 있는 경우 마이그레이션/롤백 전략 확인
